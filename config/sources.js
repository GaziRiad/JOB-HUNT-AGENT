// Toggle aggregator feeds. ATS boards (config/companies.js) are always on.
// Turn any of these off if it gets flaky; the run still uses the rest.
export const sources = {
  remoteok: true,
  remotive: true,
  arbeitnow: true,
  himalayas: true,
  wwr: true,
  hn: true,
};
